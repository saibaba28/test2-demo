# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

stages: 
- stage: BUILD_STAGE
  jobs:
  - job: demo
    steps:
      - bash: echo "Stage build"

- stage: RELEASE_A
  variables:
   - name: var
     value: True
  jobs:
  - job: test_01
    steps: 
      - task: Bash@3
        inputs:
          targetType: 'inline'
          script: |
                        - script: echo "start"
                        - ${{ if eq(variables.var, 'True') }}:
                        - script: echo "this is adaptum"
                        - ${{ elseif eq(variables.var, 'False') }}: 
                        - script: echo "this is contoso" 
- stage: RELEASE_B
  jobs:
  - job: test_02
    steps:
     - bash: echo "Stage C"   
    


